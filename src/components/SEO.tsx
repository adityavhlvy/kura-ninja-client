export default function SEO(_props: any) {
    // In Next.js App Router, metadata is handled via the exported metadata object 
    // or generateMetadata function in page.tsx. This component is kept to prevent 
    // breaking changes in legacy view components, but it renders nothing.
    return null;
}
