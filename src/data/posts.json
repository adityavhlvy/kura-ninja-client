{
  "posts": [
    {
      "slug": "smart-model-routing-with-jev",
      "title": "A triage step for AI models, built on Jev",
      "date": "2026-09-29",
      "summary": "How I used TypeSafe's new System One model, Jev, to pick the right model for every message in PINTER, the trade-offs I chose, and what I would change next.",
      "cover": "/assets/smart-model-routing-with-jev/cover.png",
      "tags": [
        "ai-engineering",
        "model-routing",
        "llmops",
        "typesafe"
      ],
      "project": "pinter",
      "minutes": 8,
      "toc": [
        {
          "id": "the-problem",
          "label": "The problem"
        },
        {
          "id": "why-not-ask-an-llm-to-pick",
          "label": "Why not ask an LLM to pick"
        },
        {
          "id": "what-a-system-one-model-is",
          "label": "What a System One model is"
        },
        {
          "id": "how-the-router-works",
          "label": "How the router works"
        },
        {
          "id": "design-decisions",
          "label": "Design decisions"
        },
        {
          "id": "what-a-fallback-looks-like",
          "label": "What a fallback looks like"
        },
        {
          "id": "one-bug-worth-sharing",
          "label": "One bug worth sharing"
        },
        {
          "id": "a-rough-cost-estimate",
          "label": "A rough cost estimate"
        },
        {
          "id": "what-is-not-measured-yet",
          "label": "What is not measured yet"
        },
        {
          "id": "what-i-would-change-next",
          "label": "What I would change next"
        },
        {
          "id": "takeaway",
          "label": "Takeaway"
        }
      ],
      "html": "<p>TypeSafe AI released Jev on 15 September 2026. It is their first &quot;System One&quot; model, and it does not write text. A week later it was the routing step in PINTER, the AI agent platform I build at Pupuk Indonesia. This post covers why I picked it, how the router works, and where it still falls short.</p>\n<h2 id=\"the-problem\">The problem</h2>\n<p>In PINTER, each agent was tied to one model. That forced a bad choice. Put a strong model behind everything and simple questions cost as much as hard ones. Put a cheap model behind everything and hard questions get weak answers.</p>\n<p>Most messages are simple. A greeting, a lookup, a short rewrite. A few need real reasoning. I wanted each message to go to the model that fits it, decided per message, without the user doing anything.</p>\n<h2 id=\"why-not-ask-an-llm-to-pick\">Why not ask an LLM to pick</h2>\n<p>The common trick is to prompt a language model: &quot;Here are the models. Reply with the name of the best one.&quot; It works in a demo. In production it has three problems.</p>\n<ul>\n<li>The answer is text. You parse it and hope it matches one of your options.</li>\n<li>There is no confidence. A guess and a sure answer look the same.</li>\n<li>You pay for a generation step to get one word back.</li>\n</ul>\n<p>A router needs a choice from a fixed list, a number that says how sure it is, and a clean way to fail.</p>\n<h2 id=\"what-a-system-one-model-is\">What a System One model is</h2>\n<p>Jev is built for that shape of problem. You send it a state, which is any text, and a set of typed questions. It answers each question with a structured value, not prose. There are three question types:</p>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Asks</th>\n<th>Returns</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Choice</td>\n<td>Which option fits?</td>\n<td>choice, probabilities, confidence</td>\n</tr>\n<tr>\n<td>Score</td>\n<td>How does this rate on a rubric?</td>\n<td>score, probabilities, confidence</td>\n</tr>\n<tr>\n<td>Noul</td>\n<td>Is this statement true?</td>\n<td>a value from 0 to 1</td>\n</tr>\n</tbody></table>\n<p>A Choice question is a router. The options are my routes, and the confidence tells my code whether to trust the answer. Questions in one call run in parallel, so adding more later barely changes the response time. Input costs $0.042 per million tokens, and there are no output tokens to pay for.</p>\n<h2 id=\"how-the-router-works\">How the router works</h2>\n<p>PINTER already had virtual models: a name that points to real models. <code>combo</code> tries models in order until one works. <code>round_robin</code> spreads load. I added a third type, <code>smart_router</code>.</p>\n<p>An admin sets it up with:</p>\n<ul>\n<li><strong>Routes.</strong> Each route has an intent key, a plain description of what belongs there, and the target model.</li>\n<li><strong>A fallback model</strong> for anything the router cannot place.</li>\n<li><strong>A confidence threshold</strong>, 0.65 by default, adjustable from 0.10 to 0.95.</li>\n</ul>\n<p>My router, <code>smart-pinter</code>, has four routes. Cheap text goes to a fast model, tool-heavy steps to a low-latency one, images to a vision model, and hard reasoning or code to the strongest model.</p>\n<figure>\n  <img src=\"/assets/smart-model-routing-with-jev/dialog-routes.png\" alt=\"Smart router settings with four intent routes. Each route has an intent key, a target model, and a description.\" loading=\"lazy\" />\n  <figcaption>Each route is an intent key, a target model, and a plain description. Jev reads the descriptions, not the keys.</figcaption>\n</figure><p>When a message comes in:</p>\n<ol>\n<li>PII in the message is masked before anything else sees it.</li>\n<li>The router sends the latest user message to Jev as one Choice question. The route descriptions are the options.</li>\n<li>Jev returns the chosen intent, its confidence, and the probability of every option.</li>\n<li>If the confidence meets the threshold, the message goes to that route&#39;s model. Otherwise it goes to the fallback.</li>\n<li>The agent runs on the chosen model and streams its answer.</li>\n<li>The decision streams to the chat as well, so the user sees it.</li>\n</ol>\n<figure>\n  <img src=\"/assets/smart-model-routing-with-jev/routing-card-confident.png\" alt=\"Chat message asking for a Python Dijkstra script. The Smart Routing card shows intent advanced_reasoning_and_code at 99 percent confidence, routed to Deepseek V4 Pro.\" loading=\"lazy\" />\n  <figcaption>A coding request. Jev picks advanced_reasoning_and_code with 0.99 confidence, well above 0.65, so the message goes to the strongest model.</figcaption>\n</figure><p>The request to Jev looks like this. The descriptions are shortened.</p>\n<pre><code class=\"language-json\">{\n  &quot;state&quot;: &quot;&lt;latest user message, PII masked&gt;&quot;,\n  &quot;model&quot;: &quot;jev-latest&quot;,\n  &quot;questions&quot;: {\n    &quot;intent&quot;: {\n      &quot;type&quot;: &quot;choice&quot;,\n      &quot;instructions&quot;: &quot;Determine the optimal handling tier for this request&quot;,\n      &quot;criteria&quot;: {\n        &quot;general_low_cost_text&quot;: &quot;High-volume, non-critical text tasks&quot;,\n        &quot;low_latency_tool_calls&quot;: &quot;Text-only tasks that need fast responses&quot;,\n        &quot;multimodal_vision_processing&quot;: &quot;Prompts with images, charts, or scans&quot;,\n        &quot;advanced_reasoning_and_code&quot;: &quot;Complex coding and multi-step reasoning&quot;\n      }\n    }\n  }\n}\n</code></pre>\n<p>The decision itself is plain code:</p>\n<pre><code class=\"language-python\">choice, confidence, probabilities = await evaluate_choice(prompt, criteria)\n\nif choice and confidence &gt;= threshold:\n    route = find_route(routes, choice)  # case-insensitive match on intent key\n    if route:\n        return route.target_model_id\n\nreturn fallback_model_id\n</code></pre>\n<p>That split is the point. Jev makes the judgment. My code decides what to do with it, and the threshold is a setting, not a prompt.</p>\n<h2 id=\"design-decisions\">Design decisions</h2>\n<p><strong>It fails open.</strong> If Jev times out after 5 seconds, returns an error, or there is nothing to classify, the message goes to the fallback. The client never raises. A broken triage step should never break the chat.</p>\n<p><strong>The threshold is a dial.</strong> A high threshold sends more traffic to the fallback. A low one trusts Jev more. Admins tune it per router without touching code.</p>\n<figure>\n  <img src=\"/assets/smart-model-routing-with-jev/dialog-decision-engine.png\" alt=\"Model group settings for smart-pinter with Smart Router selected, TypeSafe Jev as the decision model, Gemini 3.5 Flash Lite as fallback, and the confidence threshold slider at 0.65.\" loading=\"lazy\" />\n  <figcaption>The decision engine, the fallback model, and the threshold live in one panel. The ~150ms in the panel is UI copy. I have not measured it.</figcaption>\n</figure><p><strong>Only the latest message is classified.</strong> This keeps the call small and the result predictable. The cost is that a short follow-up like &quot;and the second one?&quot; is judged without its context.</p>\n<p><strong>Private data stays masked.</strong> Jev receives the message after PII is replaced with tokens.</p>\n<p><strong>Keys live in the vault.</strong> The Jev API key can come from a registered model, so it is not stored in the router config. When the config is read back, the key is masked.</p>\n<p><strong>No nesting.</strong> A route must point to a real model, not another virtual one. That keeps every decision one hop deep and easy to explain.</p>\n<p><strong>The decision is visible.</strong> Each answer carries a Smart Routing card: the intent, confidence against the threshold, and a bar for every option. It is saved with the message, so it shows up in history and shared chats too.</p>\n<h2 id=\"what-a-fallback-looks-like\">What a fallback looks like</h2>\n<p>Not every message fits one route. I asked for a map of fertiliser demand across Indonesia plus a chart of sales trends. Jev&#39;s best guess scored only 0.14, far below 0.65. My guess is that the request mixes data lookup, tool calls, and visual output, so no single description matched it well.</p>\n<figure>\n  <img src=\"/assets/smart-model-routing-with-jev/routing-card-fallback.png\" alt=\"Chat message asking for a demand map and a sales chart. The Smart Routing card shows Fallback 14 percent below 65 percent, followed by four completed tool calls.\" loading=\"lazy\" />\n  <figcaption>Low confidence, so the router falls back to Gemini 3.5 Flash Lite. The agent still runs its tools.</figcaption>\n</figure><p>The fallback model handled it. It listed the datasets, queried them, and drew both visuals.</p>\n<figure>\n  <img src=\"/assets/smart-model-routing-with-jev/fallback-map.png\" alt=\"Choropleth map of Indonesia titled Sebaran Demand Pupuk Indonesia per Provinsi, with Jawa Timur highlighted at 1,850,000.\" loading=\"lazy\" />\n  <figcaption>Fertiliser demand per province, from the fallback path.</figcaption>\n</figure><figure>\n  <img src=\"/assets/smart-model-routing-with-jev/fallback-line-chart.png\" alt=\"Line chart of monthly sales from January to May, peaking at 850,000 in April.\" loading=\"lazy\" />\n  <figcaption>The sales trend chart from the same answer.</figcaption>\n</figure><p>This is the case the fallback exists for. It gets every message the router is unsure about, so it has to handle any kind of request, including tool calls. The low score is also a signal: requests like this may deserve their own route.</p>\n<h2 id=\"one-bug-worth-sharing\">One bug worth sharing</h2>\n<p>The first version never matched a single route. Jev was answering correctly. The problem was the config encryptor. It encrypted every field whose name contained &quot;key&quot;, to protect secrets. <code>intent_key</code> matched that rule, so every route name was stored encrypted and never equal to Jev&#39;s answer. The fix was a short list of exempt field names, plus a test that route names stay readable.</p>\n<h2 id=\"a-rough-cost-estimate\">A rough cost estimate</h2>\n<p>I have not measured savings yet, but list prices give a first idea. The assumptions:</p>\n<ul>\n<li>3,000 input and 600 output tokens per message.</li>\n<li>A traffic mix I guessed: 40% cheap text, 20% tool calls, 10% vision, 15% hard reasoning, 15% fallback.</li>\n<li>Public list prices on 26 September 2026, off-peak for DeepSeek.</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Setup, per 10,000 messages</th>\n<th>Cost</th>\n<th>Smart router compared</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Smart router, Jev included</td>\n<td>$21.06</td>\n<td>-</td>\n</tr>\n<tr>\n<td>Everything on Gemini 3.8 Flash</td>\n<td>$45.00</td>\n<td>53% cheaper</td>\n</tr>\n<tr>\n<td>Everything on Deepseek V4 Pro</td>\n<td>$31.68</td>\n<td>34% cheaper</td>\n</tr>\n<tr>\n<td>Everything on Gemini 3.5 Flash Lite</td>\n<td>$24.00</td>\n<td>12% cheaper</td>\n</tr>\n<tr>\n<td>Everything on DeepSeek V4.1 Flash</td>\n<td>$8.10</td>\n<td>2.6x more expensive</td>\n</tr>\n</tbody></table>\n<p>Three things stand out.</p>\n<ul>\n<li><strong>Jev is close to free.</strong> It adds $0.17 per 10,000 messages, under 1% of the bill. It only reads the latest message and has no output tokens.</li>\n<li><strong>The saving depends on the baseline.</strong> Against the strongest model, the router saves about a third. Against the cheapest model, it costs more. What it buys there is quality on the hard messages.</li>\n<li><strong>The fallback is the biggest lever.</strong> At these prices Gemini 3.5 Flash Lite costs more per message than Deepseek V4 Pro, because its output tokens cost more. With 15% of traffic landing there, the fallback choice moves the bill more than any route.</li>\n</ul>\n<p>The dollar amounts are small at this scale. The ratio is what matters, and it only holds if the guessed mix is close to real traffic.</p>\n<h2 id=\"what-is-not-measured-yet\">What is not measured yet</h2>\n<p>I would rather say this plainly than guess.</p>\n<ul>\n<li><strong>Latency.</strong> I have not benchmarked the extra time the triage step adds.</li>\n<li><strong>Cost.</strong> The estimate above uses a guessed traffic mix. I have not compared real spend before and after routing.</li>\n<li><strong>Accuracy.</strong> Routing has not been scored against a labelled set of real messages.</li>\n</ul>\n<p>These are the next things to measure, and they decide whether the thresholds are right.</p>\n<h2 id=\"what-i-would-change-next\">What I would change next</h2>\n<ul>\n<li><strong>Retry on the fallback in chat.</strong> The OpenAI-compatible endpoint already tries the fallback when the chosen model fails. The chat path does not yet. Both should behave the same.</li>\n<li><strong>Track the fallback rate.</strong> A fallback today only writes a warning to the log. A rising fallback rate is the first sign that routes or thresholds need work, so it should be a metric with an alert.</li>\n<li><strong>Ask more than one question.</strong> Jev evaluates questions in parallel, so one call could return a Choice for the topic and a Score for complexity. My code would combine them. Changing priorities would mean changing a number, not rewriting a prompt.</li>\n<li><strong>Give follow-ups some context.</strong> Classifying the last two or three messages together would fix the &quot;and the second one?&quot; case.</li>\n</ul>\n<h2 id=\"takeaway\">Takeaway</h2>\n<p>Choosing a model is a decision, not a conversation. Once I treated it that way, the router became small: one typed question, one number, and a few lines of code that decide what to trust. Jev is new, but that idea is the part I would keep with any tool.</p>\n"
    }
  ]
}